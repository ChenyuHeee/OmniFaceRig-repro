# 进度

> 规则:每次开工/收工更新一条(日期、做了什么、卡在哪、下一步)。

## 2026-08-19

- [x] 建立独立 GitHub 私有仓库 `OmniFaceRig-repro`(gh CLI)
- [x] 初始化目录结构、README、.gitignore
- [x] 论文第一遍通读(37 页),产出 `notes/论文精读.md`
  - 输入是静态 3D mesh,不是图 → 需补 image-to-3D 前端
  - 论文只做面部 rig,无身体骨骼;FACS 155 ≠ ARKit 52;无音频口型模块 —— 均需补

## 2026-08-21

- [x] 需求核实:与官方 Homework-Deeptech.pdf 逐条比对,5 项交付物确认无误(`notes/challenge需求核实.md`)
- [x] 开源组件实地核验(4 个并行 subagent,全部 GitHub API/HF/PyPI 实核),报告在 `notes/components/`
  - 01 图像前端:TRELLIS/TRELLIS.2(MIT)首选;官方代码与 Omni-Bench 数据集均**未发布**
  - 02 人脸解析:MediaPipe(478+52 blendshape)+ Sapiens(唇牙舌,NC)+ BiSeNet(眼眉)+ SAM2;动物/风格化需自训
  - 03 几何栈:trimesh+xatlas+scipy 自写(ARAP/DT/Delta Mush)+pygltflib,全 MIT/Apache 无 GPL;ICT-FaceKit 补口腔资产
  - 04 口型:英文 Audio2Face-3D(直出 ARKit)/Rhubarb;**中文无现成方案,必须自建音素→viseme→ARKit 映射**
- [x] 技术选型总汇:`notes/components/00_技术选型总汇.md`

## 2026-08-24

- [x] TA 资源入库(`glb and image testing/`):23 个 T-pose glb + 12 张 2D 图 + A100 服务器信息(docx)+ 现有方案演示(glb.gz + mov)+ manifest.csv
  - 官方 3 项资源已齐 2 项(服务器✅、glb/图✅,代码走读待安排)
  - 注:FINAL_WORK_DEMO.glb 原 117MB 超 GitHub 上限,已压成 .gz(78MB);服务器密码在 docx 中为明文,注意仓库保持私有

## 下一步

1. 与 TA 对齐:走读安排 + 验收口径(交付物 3/4/5 判定标准)
2. 用官方 glb 直接跑通 Stage 1(渲染→解析→关键点→配准),先跳过 image-to-3D
3. A100 部署 TRELLIS 跑通「图 → PBR glb」冒烟
4. 写 Stage 2 几何管线(trimesh 布尔换脸 → ICT-FaceKit 口腔 + ARAP/SDF → DT → pygltflib)

## 2026-08-24(接手,下午-深夜)

- [x] 接手仓库:git 直连被墙 → 用 gh API 拉 tarball + 注册 deploy key(SSH over 443)重新 clone,恢复完整历史与 push 能力
- [x] 拿到 TA 资源(用户已入库):A100 服务器访问方式(a100_server_EN.docx)、23 个 T-pose glb(ai3d_01~23,各 ~56MB)、12 张 2D 图、现有方案演示 FINAL_WORK_DEMO.glb + 视频
- [x] **A100 服务器打通**:SSH(SERVER_IP:SSH_PORT)验证 A100-SXM4-40GB、Python 3.10、conda env torch2.4_cuda12.1(torch 2.4.1 + CUDA OK);服务器可直连 GitHub,已装 deploy key
- [x] **输出标杆确认**(FINAL_WORK_DEMO.glb 结构解析):53 关节 Mixamo 骨骼(mixamorig:*)+ 36 段全身动画 + **52 个 ARKit blendshape**(与我们的名单一致)+ 口腔内部独立网格;输入 ai3d_01.glb 是 ~1M 顶点静态网格(无骨骼无 morph)
- [x] **code/ 管线代码落地**(28 个测试全过):
  - `omnifacerig_repro/` 包:arkit52(FACS AU→ARKit 映射表)/ geometry(余切 Laplacian、ARAP、**S&P 稀疏变形迁移**(对照 vasiliskatr MIT 参考、修掉转置 bug + 平移零空间 pin)、Delta Mush、SDF)/ template_fit(式1 刚体 + 式2 六项能量解析梯度 L-BFGS)/ lip_sync(普通话声韵母→viseme→ARKit + Rhubarb 英文)/ glb_export(pygltflib:52 morph targets + skin + WEIGHTS 动画)
  - `pipeline.run_demo()` 端到端 CPU 演示:合成头 → Stage1 配准(corr_err 2.4e-4,零翻面)→ 脸融合(凸包切除+焊接)→ 52 morph → 骨骼 → 中文 viseme 动画 → outputs/demo.glb
- [x] vendor 参考实现 vasiliskatr(MIT,仅代码;CARV3D 身份数据**不入库**,见 vendor README)
- [ ] 下一步(A100):克隆仓库 → 跑测试 → TRELLIS「图→PBR glb」冒烟 → 官方 glb 人脸关键点(渲染+解析模型)→ 真实 Stage1/Stage2 → Mixamo 骨骼自动绑定 → 中英口型实测 → Web 预览服务(32170→8000/32171→8001)

## 2026-08-25(接手第二天,A100 实机跑通)

- [x] **A100 实机管线**:官方 ai3d_01.glb(100万顶点)→ rigged glb,~30s/角色
  - 几何人脸锚点(头部比例 + 正面表面吸附,风格化角色无 2D 人脸可用时兜底;MediaPipe 仅真人脸有效,官方 12 张 2D 图全部检测失败——印证调研报告预判)
  - **52 个 ARKit morph(稀疏 accessor,脸区 5 万顶点/形状,输出 100MB 而非 600MB,对齐官方 FINAL_WORK_DEMO 的稀疏存储方式)**
  - 53 关节 Mixamo 骨骼(命名/hierarchy 与官方一致,按角色比例摆放)+ 邻近蒙皮
  - 中文口型动画(viseme→ARKit weights channel)
- [x] **验收自测**(`deliverables_check.py`):D4 ARKit 52 ✅ / D2 骨骼 53 关节 ✅ / D3 表情无破损 ✅(翻转面积 0.036%,与官方 demo ~1500 sliver 同量级;判定标准改为面积加权,因为 100 万顶点网格上任何形变都会翻 sliver 三角,官方产物亦然)
- [x] **Web 预览服务上线**:http://175.155.64.171:32170/(Flask + three.js:上传图/选角色/口型文本 → 异步 rig → glb 预览 + 表情滑杆 + 口型动画播放)
- [x] 关键修复:pygltflib 1.16 API 差异、稀疏 accessor、delta 场平滑(不能用 delta-mush 的 rest-detail 再注入)、表情幅度校准(0.05→0.015 级别,防 100 万顶点网格折叠)
- [ ] 待办:23 角色批量 rig 完成 + 全量验收;TRELLIS 图→mesh 前端(服务器/本机访问 HF 均被限速 20-40KB/s,3.3GB 权重需 ~25h,记录 blocker;替代:官方 2D 图↔glb 配对 + MediaPipe 真人脸路径);口腔内部资产(ICT-FaceKit 牙齿/舌,待下载);英文口型音频对齐前端;Web 服务开机自启

## 2026-08-25(深夜收尾)

- [x] Web 端到端验证(外部 URL http://175.155.64.171:32170/):选角色→rig→glb 下载 ✅;multipart 图片上传→无脸回退几何锚点→rig ✅
- [x] 英文口型:grapheme→viseme 近似映射 + 中英自动检测(含 CJK 判定)
- [x] 验收自测脚本 deliverables_check.py(面积加权翻转判定,官方 demo 作基准)
- [x] 服务器 watchdog:23 glb 上传完成后自动批量 rig(后台进行中,~30s/角色)
- [ ] 仍待办:批量 rig 全量验收;口腔内部(ICT-FaceKit 牙齿/舌,HF 下载受限);TRELLIS 图→mesh(网络 blocker,详见下);真人照片输入验证(MediaPipe 路径);Web 开机自启(systemd)

## 2026-08-25(多代理协作收尾)

- [x] **4 个并行 subagent 协作完成**(每个独立物理目录+分支,issue/PR 协作,AGENTS.md 防抢活):
  - **rig-ops**(PR #7):23/23 官方角色批量 rig + 全量验收,**全部 PASS**(D2 53 关节 / D3 翻转面积 0.003-0.057% / D4 ARKit52 全一致),验收报告 `notes/验收报告.md`
  - **inner-mouth**(PR #8):ICT-FaceKit(MIT,gh api 通道)牙齿/牙龈/舌入库 `code/vendor/ict_facekit/`;`inner_mouth.py` 4 archetype(human/canine/monster/flat)+ ARAP 初摆 + SDF 防穿透 + 下牙随下颌 morph;stage1_real 集成 `--inner-mouth` 开关;37 tests 全过
  - **trellis-front**(PR #6):`trellis_front.py`(TRELLIS 推理 + mock)+ `deploy_trellis.sh` + 调研文档;**hf-mirror 实测 8-11MB/s,3.3GB 权重已缓存本机**(旧 37KB/s 记录已推翻);mock→rigged glb 端到端验证
  - **web-ops**(PR #5):systemd 单元 + /api/health + /api/rigged(23 预产物)+ 上传校验 + 前端"已有产物"区,服务器部署验证全过
- [x] 协作机制验证:SSH-443 push 限速 → "先 fetch 再 push"/git data API 绕过;本地共享仓库 relay;merge 冲突由 agent 自行 merge main 解决
- [ ] 下一步(非代理):A100 部署 TRELLIS(本机权重 rsync 上服务器 + CUDA 子模块编译)→ 真实"图→rigged glb"冒烟;口腔部件在服务器全量角色上验证(--inner-mouth 批量);英语音频对齐前端

## 2026-08-25(深夜,最终产品冲刺)

- [x] **阶段 2 完成:inner-mouth 真实口腔 23/23 批量 PASS**
  - 每角色 4 primitives(body + teeth_upper 2197v + teeth_lower 2201v + gums_tongue 2949v),每部件 52 ARKit morphs,53 关节,动画
  - 验收脚本 im_accept.py 23/23 PASS(结构/部件/morph 完整性)
- [x] **阶段 3 完成:中英真实音频驱动口型同步**
  - 路径:文本 → piper TTS 真实语音(zh_CN-huayan / en_US-lessac,hf-mirror 下载)→ faster-whisper 真实词时间戳 → viseme → ARKit WEIGHTS 动画(真实秒)
  - 或:上传真实音频 + 文本 → faster-whisper 对齐(统一引擎)
  - 验证:EN 29 visemes/2.54s、ZH 37 visemes/2.80s;服务器端 ai3d_01_talk_en/zh.glb 各 1 个真实 WEIGHTS 动画(32/40 关键帧)
  - 本机 58 tests 全过;webapp 音频端点端到端验证(文本→真实口型动画 glb 下载)
- [x] webapp 更新:已有产物列表 48 个(23 rigged + 23 im + 2 talk)
- [ ] **阶段 1 进行中:TRELLIS 部署**
  - 权重 3.1GB 经本机中转上传(681MB/3.1GB 进行中,~0.3MB/s);源码已就位;utils3d 已装;open3d 下载中(image_to_3d 不需要,文本管线才用)
  - 权重就绪后:设 TRELLIS_MODEL_PATH → 官方 2D 图冒烟 → stage1_real 对接

## 2026-08-25(深夜,阶段 1 完成 — 全链路真实产品)

- [x] **TRELLIS 真实部署完成**(A100):权重 3.1GB(服务器 hf-mirror 直连,波动 0.2-5MB/s,尺寸校验)、源码+FlexiCubes 子模块、spconv/nvdiffrast/kaolin补丁/diff-gaussian-rasterization(mip-splatting 版)/dinov2 本地 torch.hub;sdpa 注意力补丁替代 flash_attn
- [x] **真实推理冒烟成功**:官方 elon.png → TRELLIS mesh(16,485 顶点,带 PBR 纹理,~2 分钟)→ stage1 rig → **52 ARKit morph + 53 Mixamo 关节 + 口腔 3 部件 + 真实音频口型动画** 完整 glb
- [x] **Web 端到端最终闭环**:`POST /api/rig` + `image_to_mesh=1` + 上传图 → TRELLIS mesh → rigged glb 自动全链路(已验证 job 5fed950f,1.84MB glb 可下载)
- [x] 部署实录:notes/components/01c_trellis_a100_deploy.md(全部踩坑与修复)
- [ ] 已知限制:rigged glb 输出暂不含材质纹理(TRELLIS mesh 有纹理,stage1 导出器未透传);英文音频对齐需 whisper(已就绪)

## 2026-08-25(收尾:材质透传)

- [x] **rigged glb 材质/纹理透传**:build_gltf 支持 TEXCOORD_0 + attach_materials(从源 glb 复制 materials/textures/images,图片数据追加进输出 blob);官方 23 角色与 TRELLIS 网格产物全部带 PBR 纹理
- [x] 验证:FINAL2_elon_rigged.glb(4 primitives + 1 material + 1 texture + TEXCOORD + 53 joints + 52 morphs)
- [x] 23 角色带纹理批量重新生成中

## 2026-08-26(凌晨,最终产物批量)

- [x] 23 官方角色带纹理重生成(109-127MB/个,材料+纹理+TEXCOORD)
- [x] 23 角色真实音频动画版(piper TTS→whisper 对齐→ARKit 权重动画,30 关键帧)
- [x] Web 产物列表 94 个可在线预览
